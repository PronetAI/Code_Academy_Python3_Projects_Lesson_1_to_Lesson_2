# Corrected Area Calculator 📏
import math

base, height = 20, 30
area= base * height / 2
print("The triangle area is", area)

length, width = 2, 12
area = length * width
print("The rectangle area is" + str(area))
    
radius = 36
area = math.pi * radius * radius
print("The circle area is", area)
